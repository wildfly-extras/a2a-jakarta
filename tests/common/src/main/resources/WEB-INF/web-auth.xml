<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- Public: agent card endpoint (exact match takes precedence over /*) -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Agent Card</web-resource-name>
            <url-pattern>/.well-known/agent-card.json</url-pattern>
        </web-resource-collection>
        <!-- No auth-constraint = unchecked -->
    </security-constraint>

    <!-- Public: test utility endpoints (prefix match takes precedence over /*) -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Test Endpoints</web-resource-name>
            <url-pattern>/test/*</url-pattern>
        </web-resource-collection>
        <!-- No auth-constraint = unchecked -->
    </security-constraint>

    <!-- Protected: everything else -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>A2A Endpoints</web-resource-name>
            <url-pattern>/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>user</role-name>
        </auth-constraint>
    </security-constraint>

    <login-config>
        <auth-method>BASIC</auth-method>
        <realm-name>A2ATestRealm</realm-name>
    </login-config>

    <security-role>
        <role-name>user</role-name>
    </security-role>
</web-app>
